# YMM4 MCP プラグイン 改善提案 & 完全自動化ロードマップ (`suggest.md`)

## 1. はじめに & 現状の評価

本プラグイン（`YMM4McpPlugin` + `mcp-server`）は、Claude等のLLMからModel Context Protocol (MCP)を経由して「ゆっくりMovieMaker4 (YMM4)」を直接操作し、ゆっくり実況・解説・茶番・ストーリー動画の生成を支援する非常に優れたシステムです。

特に以下の点は従来の動画編集自動化ツールと比較しても極めて先進的です：
- **実音声長（`length`）による重なり自動回避**: AquesTalk音声合成後の実フレーム長をC#側から返し、セリフの被りを根絶。
- **マルチモーダル認識 (`watch` / Gemini動画解析)**: 画面キャプチャとWASAPIループバック録音により、AIが動画の「映像」と「音声」を直接確認可能。
- **リフレクション経由の全機能アクセス (`ymm4_advanced`)**: 専用APIが未実装な機能に対しても `MainViewModel` や `ActiveTimeline` へ動的アクセス可能。

しかし、「AIによる動画制作の完全自動化」や「より高クオリティな動画生成」を実現するためには、まだいくつかの機能やAPIが不足しています。本文書では、不足している機能と完全自動化に向けた要件を整理して提示します。

---

## 2. プラグインに足りないもの・必要な機能 (Feature Gap Analysis)

### 2.1 メディア素材（動画・画像・音声/BGM/効果音）の直接追加 API
* **現状**: 立ち絵・セリフ・テキスト・表情の追加エンドポイントは存在しますが、外部の動画ファイル（`.mp4`, `.avi`）、画像ファイル（`.png`, `.jpg`）、音声ファイル（`.mp4`, `.wav`, `.mp3` / BGM・SE）をタイムラインへ直接追加する専用エンドポイントが不足しています。
* **必要とされる機能**:
  - `POST /api/items/video`: 動画ファイルを指定レイヤー・フレームに追加
  - `POST /api/items/image`: 画像ファイルを指定レイヤー・フレーム・長さで追加
  - `POST /api/items/audio`: BGM/効果音ファイルを指定レイヤー・フレーム・音量で追加

### 2.2 動画書き出し（MP4レンダリング / エンコード）自動化 API
* **現状**: プロジェクトの保存（`POST /api/project/save`）は可能ですが、YMM4の動画出力（MP4等のエンコード処理）を外部から実行・監視する機能がありません。
* **必要とされる機能**:
  - `POST /api/project/export`: 出力パス、フォーマット（MP4等）、プリセットを指定してエンコードを開始
  - `GET /api/project/export-status`: レンダリングの進捗率（%）や完了/エラー状態を取得
  - レンダリング完了時の非同期イベント/通知

### 2.3 プロジェクト管理ライフサイクル API
* **現状**: 現在開いているプロジェクトに対する操作が中心であり、新規プロジェクトの作成や既存プロジェクトの切り替えがAPI経由で行えません。
* **必要とされる機能**:
  - `POST /api/project/new`: 指定のプリセット（解像度・FPS）で新規プロジェクトを作成
  - `POST /api/project/open`: 指定の `.ymmp` プロジェクトファイルを開く
  - `POST /api/project/save-as`: 名前を付けて保存

### 2.4 演出・装飾・図形・トランジション操作機能
* **現状**: テキストアイテム追加は可能ですが、フォント・サイズ・縁取り・装飾スタイル（テンプレート）の適用や、図形・場面転換（トランジション）の追加が個別エンドポイントとして用意されていません。
* **必要とされる機能**:
  - `POST /api/items/shape`: 矩形・円・矢印・マスクなどの図形アイテム追加
  - `POST /api/items/transition`: フェード、ワイプ、カーテン等のトランジションエフェクト設定
  - `POST /api/items/text/style`: 字幕テンプレート・テキストスタイルの適用

### 2.5 キーフレーム・イージング・アニメーション制御
* **現状**: アイテムの固定プロパティ（位置・拡大率・回転・不透明度）は変更可能ですが、時間経過に伴うアニメーション（キーフレーム打刻やイージング補間）の制御APIが不足しています。
* **必要とされる機能**:
  - アイテムのプロパティ（X, Y, Scale, Opacity）に対するキーフレーム追加・削除・変更
  - イージング関数（直線、イーズイン、イーズアウト、バウンス等）の指定

### 2.6 プラグイン UI & 運用・診断機能の強化
* **現状**: C#プラグインUIは起動/停止ボタンとログ表示のみです。
* **必要とされる機能**:
  - UI上でのポート番号変更・WebSockets対応
  - 接続中のMCPクライアント一覧とアクティブなAPI呼び出し履歴の可視化
  - サーバーのワンクリック再起動機能
  - YMM4内部で例外が発生した際のエラー詳細ログビューア

### 2.7 MCPツールの構成最適化
* **現状**: `ymm4_interact` が多数のアクションを保持しており、ツール定義が肥大化しています。
* **必要とされる機能**:
  - ツール群のモジュール分割（例: `ymm4_media`, `ymm4_timeline`, `ymm4_project`, `ymm4_render`）によるLLMのトークン消費削減と呼び出し精度の向上。

---

## 3. 「動画制作の完全自動化」に必要な要素 (Requirements for Full Automation)

AIがプロンプト1つから「企画・台本作成 → 素材準備 → 編集・演出 → 検品 → MP4出力」までを人間なしで完結する**完全自動化**を実現するためには、以下の仕組みが必要です。

```
 [人間/AI] 題材・テーマ入力
     │
     ▼
 ┌─────────────────────────────────────────┐
 │ 1. 企画・台本・構成案生成 (Claude / LLM) │
 └─────────────────────────────────────────┘
     │
     ▼
 ┌─────────────────────────────────────────┐
 │ 2. 素材（動画/画像/BGM/SE）の自動準備   │
 │    (画像生成AI, Web検索, BGMライブラリ) │
 └─────────────────────────────────────────┘
     │
     ▼
 ┌─────────────────────────────────────────┐
 │ 3. YMM4 タイムラインへ一括配置           │
 │    - 背景/動画/BGM/SE/キャラ/セリフ配置 │
 │    - 音声実長に基づく重なり防止・すき間調整│
 └─────────────────────────────────────────┘
     │
     ▼
 ┌─────────────────────────────────────────┐
 │ 4. 自動検証 & AI自己修復ループ (QA Loop)│
 │    - watch / Gemini動画解析による確認  │
 │    - テロップ重なり/無音/見切れの自動修正 │
 └─────────────────────────────────────────┘
     │
     ▼
 ┌─────────────────────────────────────────┐
 │ 5. 自動MP4エンコード & 完成通知         │
 └─────────────────────────────────────────┘
```

### 3.1 タイムライン同期とイベント完了制御
- **課題**: AquesTalkの音声合成や画像・動画のデコード、レンダリングには非同期の遅延が発生します。
- **要件**: C#プラグイン側で「非同期処理の完全完了」を保証し、APIレスポンスとして処理完了フラグと確定結果（実際の長さや描画領域）を返す強固な同期メカニズムが必要です。

### 3.2 AIによる自動視覚・音響検品 (Visual/Audio QA Loop)
- **課題**: セリフを追加しても、背景画像や他のテロップと被ったり、効果音の音量が大きすぎたりする問題が発生します。
- **要件**:
  1. `watch` や `export-clip` を活用し、生成されたタイムライン区間を画像・音声として書き出し。
  2. マルチモーダルAI（Gemini 2.5 Pro等）がレイアウト異常・字幕被り・無音区間・BGMとのバランスを評価。
  3. 問題点を検出した場合、AIが自動で `edit_item` や `shift` を呼び出して修復を試みる**自己修復フィードバックループ**の確立。

### 3.3 音響最適化（BGMダッキング & SE同期）
- **課題**: セリフ発話中にBGMの音量が大きいと声が聞き取りにくくなります。
- **要件**: セリフアイテムが存在するフレーム範囲において、BGMの音量を自動で下げる「BGMダッキング機能」の自動適用。

---

## 4. 推奨開発ロードマップ

### 🚀 Phase 1: 基本機能の完結（短期）
- [ ] 動画・画像・音声アイテム追加API (`/api/items/video`, `/api/items/image`, `/api/items/audio`) の実装
- [ ] MP4動画書き出しAPI (`/api/project/export`, `/api/project/export-status`) の実装
- [ ] 新規プロジェクト作成・既存プロジェクトオープンAPIの実装

### 🎨 Phase 2: 演出 & 制作クオリティ向上（中期）
- [ ] 図形・トランジション・字幕スタイル適用APIの実装
- [ ] プロパティのキーフレームアニメーション・イージング制御APIの追加
- [ ] MCPツールの機能別モジュール分割（`ymm4_media`, `ymm4_project`, `ymm4_timeline` 等）

### 🤖 Phase 3: 完全自動化パイプライン構築（長期）
- [ ] AI自動検品ループ（視覚・音響の自己修復機能）のプロンプト・スキル標準化
- [ ] セリフに連動したBGMダッキング・効果音自動配置エンジンの開発
- [ ] 外部素材生成AI（FLUX, ElevenLabs, Suno等）とYMM4 MCPの統合ワークフロー作成

---
*作成日: 2025年3月*
*対象システム: YMM4 MCP Plugin (`YMM4McpPlugin` / `mcp-server`)*
