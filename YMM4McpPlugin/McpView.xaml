<UserControl x:Class="YMM4McpPlugin.McpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:YMM4McpPlugin"
             MinWidth="500" MinHeight="500">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル・ステータス -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="🤖 YMM4 MCP連携サーバー"
                       FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusText}" Margin="12,0,0,0"
                       Foreground="{Binding IsRunning, Converter={x:Static local:BoolToBrushConverter.Instance}}"
                       VerticalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- URL表示 -->
        <Border Grid.Row="1" Background="#1e1e2e" CornerRadius="4"
                Padding="8,6" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="{Binding BaseUrl}" Foreground="#cdd6f4"/>
                <StackPanel IsEnabled="{Binding CanConfigure}" Margin="0,8,0,0">
                    <TextBlock Text="ポート (1024〜65535)" Foreground="#cdd6f4"/>
                    <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}"/>
                    <CheckBox Content="次回からプラグイン読込時に自動起動" IsChecked="{Binding AutoStart}" Foreground="#cdd6f4"/>
                    <CheckBox Content="危険な高度APIを許可（任意リフレクション）" IsChecked="{Binding AllowAdvanced}" Foreground="#cdd6f4"/>
                    <Button Content="設定を保存" Command="{Binding SaveSettingsCommand}"/>
                </StackPanel>
                <TextBlock Text="record/watchは他アプリを含むシステム音声全体を録音します。" Foreground="#f9e2af" TextWrapping="Wrap"/>
                <TextBlock Text="接続情報（秘密トークン含む・共有禁止）:" Foreground="#cdd6f4"/>
                <TextBlock Text="{Binding ConnectionPath}" Foreground="#cdd6f4" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- ログエリア -->
        <Border Grid.Row="2" Background="#181825" CornerRadius="4"
                Padding="4" Margin="0,0,0,8">
            <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock"
                           Text="{Binding Log}"
                           Foreground="#a6e3a1" FontFamily="Consolas" FontSize="11"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- ボタン群 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="ログクリア" Command="{Binding ClearLogCommand}"
                    Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="■ 停止" Command="{Binding StopCommand}"
                    Padding="12,6" Margin="0,0,8,0"
                    Background="#f38ba8" Foreground="White"/>
            <Button Content="▶ 起動" Command="{Binding StartCommand}"
                    Padding="12,6"
                    Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
